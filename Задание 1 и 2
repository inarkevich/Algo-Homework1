Задание 1. 

int calc(int* arr, int size)
{
  int ans = 0;
  for (int i = 1; i < size; i++)
  {
    ans += arr[i] - arr[i-1];
  }
  return ans;
}

1. Данный код описывает функцию, выполняющую операции с элементами заданного массива. Функция принимает в качестве аргументов указатель на массив и его размер. 
Цикл for пробегается по массиву, вычитает из каждого последующего элемента предыдущий, и складывает полученные разницы в одну обшую переменную ans. В качестве возвращаемого элемента выводится 
переменная ans. 
2. Aссимптоматика : время - О(n) - линейная, тк с увеличением размера в 100 раз массива время на выполнение функции так же увеличится в 100 раз. Память О(1) - константная, тк мы используем 
всего одну переменную ans.

Задание 2. 


